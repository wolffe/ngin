# Models

| File | Use |
|------|-----|
| `low_poly_building.glb` | Static building prop in the sandbox |

Place with `placeGltf` (`src/assets/placeGltf.js`), not by adding `gltf.scene` raw.

The third-person player is procedural R6 geometry (`src/player/CharacterModel.js`), not a GLB.
